# ComfyUI / RunningHub 短剧资产生成管线 · Agent 化架构方案（完整版）

> 本文档由 2025-09-03 的 pi 对话整理而成，是项目 agent 化改造的纲领性文档。
> 配套存档：`pi对话存档_agent化架构讨论.md`（完整对话记录）。
> 保留原因：新对话中的 AI 无法自动继承旧对话记忆，此文档承载关键上下文。

---

## 一、项目现状（事实确认）

- **工作流在 RunningHub 云端**：4 个工作流（prompt / char / prop / scene）已发布在云端，用 `workflowId` 引用（见 `generation.py` 顶部常量）。
- **本地 `workflows/*.json` 只是设计期存档副本，运行期完全不读**。
- **与云端交互是节点级 API 调用**：
  - `POST /task/openapi/create`：`workflowId + nodeInfoList[{nodeId, fieldName, fieldValue}]` 提交任务
  - `POST /openapi/v2/query`：轮询状态 + 下载结果文件
- **现有节点参数暴露**（generation.py `NODE_CONFIG`）：prompt 工作流节点 27/28；char/prop/scene 的 103/58/242(String)。
- **现有管线**（`ai-short-drama-pipeline/`）：
  - `launcher.py` 编排 6 步：生成 → 质检 → 人体完整性检测 → 返工 → 报告 → 假设检验
  - `generation.py`：剧本解析、提示词生成、并发提交、下载合并（5 路并发，2h 超时）
  - `core_eval.py`：三层质检（IQA 画质 / CLIP 语义 / 专项硬约束）
  - `rework.py` / `reporting.py` / `analysis.py` / `lora_grid_analysis.py`

---

## 二、关键术语澄清（讨论结论）

| 术语 | 含义 | 一句话 |
|---|---|---|
| LLM | 大语言模型 | 大脑 |
| Agent | 智能体 | 会用工具做事的 LLM |
| Agent Harness | 代理鞍具/代理框架 | 套在 LLM 外的完整外壳：循环、工具、UI、上下文、会话（pi 就是） |
| Agentic AI | 代理式 AI | 强调自主行动的 AI 体系 |
| Agentless | 无代理 | 不引入 agent 框架，代码直编（本项目 launcher 现状） |
| Workflow | 工作流 | 代码拥有控制循环 |
| ReAct | 推理-行动循环 | agent 的思考机制之一 |

**核心区分**（业界定义）：
> workflow：LLM 和工具被预设的代码路径编排。
> agent：LLM 动态控制自己的过程和工具使用，掌握完成任务的主动权。

**判据**：删掉 LLM，系统还能跑 → 是 workflow；不能 → 是 agent。
**重要**：LangGraph 也能加决策层甚至 ReAct agent，框架不是边界；边界在于"决策点交给谁"+"动作空间多大"。

---

## 三、三个层级的定位（讨论结论）

| 层级 | 代表 | 定位 | 类比 |
|---|---|---|---|
| 库/框架 | LangChain / LangGraph | 你 import 的代码，可嵌进任何系统 | 发动机零件 |
| Harness | Pi / Claude Code | 完整执行环境：循环+工具+UI+会话 | 整车+驾驶员 |
| 平台 | 扣子 | 云托管全套服务 | 租车公司 |

**核心结论**：
- LangGraph 是 framework（半成品零件箱，自己组装）；pi 是 harness（成品，往里加工具和上下文即可）。
- **LangGraph/LangChain 的代码可以被 pi 作为工具调用**（`pi.registerTool` 里 spawn 或调 API）——不是二选一，是分层组合。
- 企业化路径：**不要 fork pi 源码**（升级冲突），用 extensions + skills + packages + SDK 组装。

### 3.5 为什么 harness 好用：能力清单（2025-09-04 补充）

**结论先行**：harness 把 agent 系统的 80% 通用复杂度（循环、协议、上下文、状态）打包成可靠的默认实现，让开发者只需投入那 20% 的领域复杂度（工具、知识、规则）——而这部分恰好是用户最懂、最想控制的部分。

**Harness 八项能力**（以 pi 为例）：

| # | Harness 能力 | 具体做什么 | 没有它要自己写 |
|---|---|---|---|
| 1 | **Agent Loop** | 思考→调用→观察→再思考 的循环控制 | 手写 while 循环 + 终止条件 |
| 2 | **工具协议** | 工具注册、参数校验、调用派发、结果回填（Function Calling 全链路） | 手写 tool_calls 解析 + 分发 |
| 3 | **上下文管理** | 历史消息、token 预算、自动压缩 | 手写 token 计数 + 截断策略 |
| 4 | **会话持久化** | 对话存盘、恢复、分支 | 手写文件格式 + 序列化 |
| 5 | **安全护栏** | 工具拦截、危险操作确认（extension 事件） | 手写每个工具的权限检查 |
| 6 | **资源加载** | extensions / skills / prompts / themes 自动发现 | 手写文件扫描 + 解析 |
| 7 | **UI 交互** | 确认框、选择器、状态展示 | 手写终端交互 |
| 8 | **多模型适配** | 各家 API 协议差异抹平 | 手写多套适配层 |

**"格式化输入"在能力表中的位置**：用户问到的"把工具清单+格式要求+系统提示+会话历史拼装成请求"，对应第 2 项"工具协议"，是 harness 能力集的入口，但不是全部。

**工具调用 JSON 的产生链路**（为什么透明）：

```
你发消息 → pi 组装请求（你的消息 + 系统提示 + 工具清单 + 格式要求）
        → LLM 原生输出结构化 JSON（tool_calls，模型的 Function Calling 能力）
        → pi 解析 arguments 字符串 → 校验格式 → 派发给对应工具 execute
        → execute 拼命令 → 执行 → 结果回填 → 回到 LLM 继续推理（循环）
```

**可审查边界**：开发者能审查的是"注册"（registerTool 的 name/description/parameters）与"执行"（execute 转发逻辑）两段；"模型如何决定"属于模型内部推理，任何 harness（pi / LangChain / Claude Code）都不干预。

**为什么好用——三层原因**：

1. **把易错处包好**：agent loop 最易出错（终止条件、结果回填、上下文爆炸、并发、超时重试），harness 提供验证过的默认实现，开发者只填空。
2. **扩展点恰好长在能力边界上**：确定性操作→写代码（registerTool）；知识按需加载→写文档（skill）；行为硬约束→写拦截（extension 事件）。三层都可控、都透明。
3. **决策权在用户手里**：确定性（代码/协议/循环）与不确定性（决策/判断/应变）清晰切开——不用在"写死"和"失控"之间二选一。

**对照本项目**：launcher.py 6 步是 workflow（确定性代码）；升级为 agent 缺的正是 harness 提供的决策层——launcher.py 变成"手"（工具），pi 的循环变成"大脑"，意外情况由 LLM 临场判断，确定性逻辑仍在代码里。

### 3.6 分层故障处理策略："解决不了"怎么办（2025-09-04 补充）

**前提**：任何"解决不了"先定位是哪一层，再决定处理方式，而不是推翻整个架构。

**四层故障分类与对应策略**：

| 层 | 典型场景 | 处理策略 | 具体手段 |
|---|---|---|---|
| A. 依赖层（Playwright 等库） | 验证码/滑块、WebDriver 指纹检测、Canvas 内元素点不到、文件对话框拦截 | **换工具，不硬扛** | 验证码→人工介入（login 存 cookie）；指纹检测→CDP 直连 + 反检测参数（已加 `--disable-blink-features=AutomationControlled`）；Canvas→坐标点击（explore 输出带 x/y）；文件对话框→`set_input_files()` 直接注入 |
| B. 动作层（rh_browser.py 业务逻辑） | 页面改版按钮文本变、需要多步操作（先展开菜单再点） | **改我们的代码，不动库** | 独立 Python 文件就是为此设计：加函数（cmd_drag / cmd_hover）+ 翻译官加工具，30 分钟搞定 |
| C. Harness 层（pi 框架能力） | 需要多步操作原子化、跨工具协调 | **沉淀成工具 / 写扩展逻辑** | `registerTool` 注册高级工具（内部循环调 6 个原语）；`pi.on("tool_call")` 拦截、`pi.on("turn_end")` 自动审计 |
| D. 决策层（LLM 判断） | 报错信息模糊、需要看图判断 | **别让 LLM 硬扛，把判断变成代码** | 报错模糊→规则匹配错误码；看图→用 core_eval.py 算指标，LLM 只读数值（架构核心原则） |

**换路径优先于硬扛**（本项目最相关）：

| 场景 | 网页路径（难） | 替代路径（易） |
|---|---|---|
| 改参数重跑 | 网页上传 JSON → 点运行 | API：generation.py 已有 `/task/openapi/create` |
| 查任务状态 | 网页轮询 | API：`/openapi/v2/query` |
| 下载结果 | 网页点下载 | API：查询接口直接给文件 |

（架构文档第十二节第 1 条铁律：工作流 JSON 运行期不用，API 节点级调用。网页操控是补充手段，主路径是 API。）

**决策流程**：

```
遇到"解决不了"的问题
  ├─ 是库能力问题？──▶ 换工具 / CDP / 人工（A）
  ├─ 是脚本逻辑问题？──▶ 改 rh_browser.py 加函数（B）
  ├─ 是框架能力问题？──▶ registerTool 沉淀工具 / extension 事件（C）
  ├─ 是模型判断问题？──▶ 把判断写成代码，LLM 只读数值（D）
  └─ 是路径选错？──▶ 切 API / 切人工，不硬扛（换路径优先）
```

**核心思想**：分层设计的价值在于——任何一层解决不了，问题都可以在另一层解决，而不是推翻整个架构。Playwright 不行有 CDP，CDP 不行有人工，网页不行有 API，LLM 不行有代码。

---

## 四、最终架构（三层 + 约束罩）

```
┌──────────────────────────────────────────┐
│ 约束框架（人设 + 护栏）                    │
│  身份层: AGENTS.md / 系统提示词           │
│  行为层: extension tool_call 拦截(硬护栏) │
│  决策层: 统计引擎 decision_rules 校验     │
│  动作空间: 只注册必要工具                 │
├──────────────────────────────────────────┤
│ Agent 决策层：pi harness（企业专有版本）   │
│  - LLM 临场决策：调参策略/异常处理/解读    │
│  - skills: 领域知识（指标注册表/管线用法）  │
├──────────────────────────────────────────┤
│ 统计评判层（护城河，用户统计学专长）        │
│  eval_engine: 指标计算 + 假设检验 + CI     │
│  + 控制图 + 多重比较校正 + 漂移检测        │
├──────────────────────────────────────────┤
│ 执行层：现有 Python 管线 / 未来 LangGraph  │
│  launcher.py / generation.py / core_eval  │
│  （状态管理/并发/审计 → 出现企业信号后升级） │
└──────────────────────────────────────────┘
```

**分层原则**：
- 确定性、可测试的流程（质检/返工/报告）→ 执行层（代码）
- 科学评判（指标/检验/区间）→ 统计层（代码，绝不让 LLM 算概率）
- 开放式临场决策 → agent 层（LLM）
- 探索必须在约束内：自由体现在"怎么演"，不体现在"演什么"

---

## 五、评价指标体系（核心新增：完整版）

### 5.1 已有指标（core_eval.py 已实现）

| 维度 | 指标 | 阈值 | 适用 |
|---|---|---|---|
| 画质 | BRISQUE | < 45 | 全部 |
| 清晰度 | 拉普拉斯模糊 | > 50 | 全部 |
| 语义 | CLIP 图文对齐 | ≥ 0.26 | 全部 |
| 人脸 | 跨图一致性 / 图内多脸一致性 | ≥ 0.72 | 人物 |
| 一致性 | 组内 CLIP 相似度 | 人物/道具 ≥0.82，场景 ≥0.65 | 按类 |
| 一致性 | pHash 感知哈希 | ≤ 15 | 组内 |
| 人体 | YOLOv8-pose 关键点 | = 17 完整 | 人物 |
| 文字 | RapidOCR 语种+关键词 | 中文占比/关键词 | 道具 |
| 构图 | 头身比 / 上下半身比 | 8~9 / 1.35~1.5 | 网格调参 |
| 风格 | Gram 矩阵距离 | 越低越好 | 网格调参 |

### 5.2 补充指标（讨论确定的 6 个高价值维度，待落地）

| # | 维度 | 方法 | 说明 |
|---|---|---|---|
| 1 | **美学评分** | LAION-Aesthetic / NIMA（0~10） | 弥补"画质合格但构图丑"盲区 |
| 2 | **手部/肢体畸形** | MediaPipe Hands 关键点 | AI 最易翻车的手指数量/结构错误 |
| 3 | **主体出框/裁切** | 检测框与画幅边界距离 | 人物/道具被边缘截断 |
| 4 | **色彩/曝光异常** | 直方图统计（过曝/欠曝比例、通道偏移） | 灰蒙蒙/过亮 |
| 5 | **重复/多样性** | 批内两两 pHash 聚类 | 防止伪多样性（大量雷同图） |
| 6 | **负向元素违规** | 按类检查（人物图有文字/道具图有人脸等） | 硬约束 |

### 5.3 指标注册表设计（agent 决策的前提）

每类资产（人物/道具/场景）一份注册表 JSON，agent **只读数值做决策，绝不看图**：

```json
{
  "asset_type": "character",
  "mandatory": [
    {"name": "brisque_score", "threshold": 45, "direction": "lt"},
    {"name": "keypoint_count", "threshold": 17, "direction": "eq"}
  ],
  "reference": [
    {"name": "aesthetic_score", "threshold": 5.0, "direction": "gt", "weight": 0.3},
    {"name": "hand_integrity", "threshold": 1.0, "direction": "eq", "weight": 0.2}
  ],
  "min_effect_size": {"aesthetic_score": 0.3},
  "ci_alpha": 0.05
}
```

- **必过项（mandatory）**：不合格直接返工，无讨论余地
- **参考项（reference）**：带权重，参与综合评分与调参方向判断
- **最小效应量 Δmin**：统计显著 ≠ 业务重要，加门槛防过度反应

---

## 六、统计评判引擎设计（核心新增）

**原则：不要让 LLM 做统计，让统计做统计，LLM 只消费"翻译好的动作建议"。**

### 6.1 统计输出 → LLM 翻译层

统计引擎输出语义信号而非原始数字（LLM 不懂频率学派，p=0.03 会被当"对/错开关"）：

```json
{
  "signal": "config_regression",
  "evidence": "p=0.031, Δaesthetic=-0.42, 超过最小效应量0.3",
  "recommended_action": "回滚Lora至0.6",
  "confidence": "medium",
  "ask_human": false
}
```

LLM 只消费 `signal / recommended_action / confidence / ask_human` 四个字段；原始统计（p 值、CI、检验统计量）留审计日志。

### 6.2 统计方法 → 评价环节映射

| 统计工具 | 用途 | 替代现状 |
|---|---|---|
| 假设检验（Mann-Whitney U） | 新配置 vs 基准显著更优 | 拍脑袋阈值 |
| Bootstrap 区间估计 | 给每批图真实质量区间而非点估计 | 单批均值 |
| 控制图（Shewhart/EWMA） | 多批均值漂移超 2σ 触发调参 | 拍脑袋"连续3批" |
| 多重比较校正（FDR） | 网格调参 121 组合显著性 | 假阳性爆炸 |
| 实验设计（混杂控制） | 同批只变一个参数 | 改 Lora 又换 seed |
| 相关性分析（PCA） | 指标去冗余 | 11 指标平权叠加 |
| 贝叶斯更新 | 累积多批证据更新参数后验 | 只看单批 |

### 6.3 必须规避的统计陷阱

1. 统计显著 ≠ 业务重要 → 加最小效应量 Δmin 门槛
2. 小样本（每批约 28 张）→ 用 bootstrap/秩检验，勿硬上正态
3. 多重比较 → 必须 FDR 校正
4. 指标共线性 → 先做相关矩阵 + PCA 降维

---

## 七、层间接口（最容易翻车的 4 个环节）

架构在纸面完善，功力在接口处。四个最易翻车环节及解法：

### 7.1 统计输出 → LLM 解读
- **风险**：LLM 把 p=0.03 当"对/错开关"，忽略效应量
- **解法**：统计引擎输出"翻译好的动作建议"（见 6.1），LLM 只消费语义信号，原始统计留审计

### 7.2 指标漂移
- **风险**：模型升级/风格更换 → 指标分布整体漂移 → 阈值失效
- **解法**：保留基准集，定期重测，漂移检测提醒再校准（见 8.3 指标健康）

### 7.3 试错成本
- **风险**：2 小时任务 × 错误决策 = 烧钱
- **解法**：高风险动作设最小间隔（防死循环）、决策前"干跑"（agent 先输出计划，确认后执行，用 ui.confirm/审批流）

### 7.4 审计粒度
- **风险**：企业要"决策链路"不是日志
- **解法**：决策账本（见 8.1），由 pi 的 tool_result/turn_end 事件 + 统计引擎决策输出拼出

---

## 八、三大工程支柱（决策账本 / 风险控制 / 指标健康）

### 8.1 决策账本（管"可信"）
每个决策点结构化落盘，形成可审计的决策链路：

```
decision_id: d-001
trigger: aesthetic CI 下限 4.8 < 5
evidence: bootstrap(1000次) CI=[4.5, 5.1], n=28
action: 回滚 Lora 0.8→0.6, 重跑 28 张
cost: ¥X, 耗时 47min
human_approved: false (规则授权)
```

### 8.2 风险控制（管"安全"）——三个刹车
- **决策间隔**：两次重跑之间强制最小间隔，防死循环烧钱
- **干跑确认**：高风险动作前，agent 先输出计划，人工确认后执行
- **人工闸**：超预算 / 返工超 N 次 → 强制停下问人

### 8.3 指标健康（管"不过期"）
- 保留"标杆图集"（已知绝对合格的图）
- 定期重测标杆，若分数整体漂移 → 阈值过期，提醒重新标定
- 模型升级 / 风格更换后必须做再校准

---

## 九、参数映射表（workflow_parameters.json，待建）

agent 能做参数决策的前提：维护"云端工作流参数映射表"，记录每个可调节点的 nodeId/fieldName/含义/取值范围：

```json
{
  "workflow_id": "2086686775652737025",
  "workflow_name": "char",
  "params": [
    {"nodeId": "103", "fieldName": "String", "meaning": "人物提示词", "editable": false},
    {"nodeId": "45", "fieldName": "lora_strength_model", "meaning": "Lora 强度", "editable": true, "range": [0.0, 1.0]}
  ]
}
```

用途：agent 拿 `lora_grid_analysis` 结果 → 通过 API 把最优参数写回云端工作流 → 触发下一轮生成（**参数调优闭环，最高杠杆**）。

---

## 十、Agent 化落地路径（顺序执行）

1. **统计引擎**：指标向量 + bootstrap CI + 假设检验封装成 `eval_engine`（核心竞争力）
2. **指标注册表 JSON**：带置信度/效应量的评判规则（阈值、权重、适用类别）
3. **pi 工具封装**：`run_pipeline` / `eval_batch` / `compare_configs` / `rh_submit` / `rh_query` / `rh_download`
4. **参数映射表**：`workflow_parameters.json`（云端可调点清单）
5. **上下文固化**：`.pi/AGENTS.md` + `evaluation` skill
6. **跑通一个剧本闭环**：验证 agent 决策优于固定规则
7. **企业信号出现后**（SLA/审计/计费/并发/审批流）→ 执行层重写为 LangGraph（编排壳重写，统计引擎和 pi 层不动）

**触发 LangGraph 层的信号**：业务方要求 SLA/审计/计费、多人并发、需要审批流、部署成团队服务。
**在此之前**：`launcher.py` + Python 脚本就是执行层，硬上 LangGraph 是白花钱。

---

## 十一、与 LangChain/LangGraph 的关系（最终结论）

- 不是竞争，是分层：**pi 是 harness（大脑+环境），LangGraph 是库（手和器官）**。
- 企业已有/未来的 LangGraph 流程，通过 `pi.registerTool()` 收编为工具，由 agent 统一调度。
- 三层护城河：模型可换、框架可换，**统计评判体系 + 领域工作流**是独有的。

---

## 十二、关键上下文备忘（本对话的重要澄清）

1. **工作流 JSON 运行期不用** —— 只在 RunningHub 云端，API 节点级调用。不要教 agent 解析本地 JSON 跑管线。
2. **让 agent 判断图片质量不可行** —— 必须可计算指标 + 统计评判，LLM 只读数值做决策。
3. **"有决策" ≠ agent 化** —— launcher 的 if/else 是自动化；把决策点交给模型才算 agent 化，且是程度不是开关。
4. **"ilness" = harnesses** —— 用户听到的术语是 agent harness（代理框架）。
5. **企业级 harness 组装方式** —— extensions（能力/护栏）+ skills（知识）+ packages（分发）+ SDK（嵌入自建系统）；不 fork。
6. **统计输出要翻译** —— LLM 不懂频率学派，只给它语义信号和动作建议，原始统计留审计。
7. **约束下的探索** —— agent 探索必须在四层约束内（身份/行为/决策/动作空间），自由度在"怎么演"而非"演什么"。
8. **指标会过期** —— 模型升级/风格更换后必须再校准，否则阈值失效。
9. **"格式化输入"是 harness 能力之一而非全部** —— 工具协议（Function Calling 全链路）只是八项能力之一；JSON 由 LLM 原生输出，harness 只解析校验派发（见 3.5）。
10. **透明边界** —— 可审查的是"注册"与"执行"两段；"模型如何决定"不可也不应审查，那是模型能力边界。
