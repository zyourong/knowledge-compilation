# Knowledge Compilation 架构设计文档

> 本文档定义"知识编译"方法论的完整设计：把专家的隐性经验编译成确定性、可执行的规则，让推理能力弱的开源小模型也能零成本执行专家任务。与训练蒸馏（改权重）不同，知识编译是推理期注入（给指令）。

---

## 一、核心洞察

### 1. 推理负担转移（Inference Offloading）
专家的隐性知识 → 编译成判定条件、成功/失败标准、每一步 if-then 写死 → **agent 只需要执行，不需要思考**。
这不是"教" agent，是"替" agent 推理。

### 2. 面向弱模型的消费哲学
主流知识框架是给**旗舰模型**用的：知识是"参考"，模型自己会思考。
知识编译是**反向设计**：知识框架的目标消费者是"笨模型"，所以结构必须傻瓜化到——
**决策树定方向、铁律挡错误、已验证清单免推理、元技能强制"直接照做，别思考"。**

### 3. 成本套利
因为知识包够"笨"、够确定，推理弱的开源模型（便宜 10-100 倍）也能干同样的活。
**知识越完整 → 需要的推理越少 → 可用越便宜的模型。**

---

## 二、为什么叫"编译"，不叫"蒸馏"

| | Knowledge Distillation（蒸馏） | **Knowledge Compilation（编译）** |
|---|---|---|
| 时机 | 训练期 | 推理期 |
| 方式 | 用大模型输出训练/微调小模型权重 | 把经验编译成确定性规则框架注入上下文 |
| 成本 | 训练成本（GPU、数据、时间） | 零训练成本，纯文档 + 注入 |
| 可审计性 | 黑盒（权重不可审查） | 白盒（规则、铁律、清单逐条可审查） |
| 可迁移性 | 每个模型要重新训练 | 同一知识包跨模型、跨 harness 复用 |
| 适用 | 通用能力迁移 | 领域经验迁移 + 任务降本 |

蒸馏改的是模型的"肌肉记忆"，编译给的是模型的"操作规程"。

---

## 三、三层元技能体系（蒸馏 → 打包 → 编译 闭环）

```
knowledge-distillation（蒸馏）   → 生产知识（思考者：分析、验证、总结、写坑）
knowledge-packaging（打包）      → 封装知识（整理者：组织成可迁移的知识包）
knowledge-compilation（编译）★   → 消费知识（执行者：直接照做，别思考）
```

### 消费知识的第一规则：直接用，笨一点（Follow, Don't Think）

| 行为 | 结果 |
|---|---|
| 照做（笨） | ✅ 复现已验证的成功 |
| 自作聪明（聪明） | ❌ 破坏已验证的流程，踩新坑 |

**认知反转**：一个"笨 agent 照着做不会错"的知识包，是最高质量的交付物。聪明用在**生产知识时**（蒸馏时想清楚），不用在**消费知识时**（执行时别多想）。

### 消费协议的 4 条准则

1. **不重新推理**：知识包说"这样连"，就这样连，不要"我觉得应该那样"
2. **不怀疑已验证结论**：实测值优先于直觉/官方文档
3. **不自作聪明**：不要"优化"已验证流程
4. **有疑问先查包，再问用户**：包里有答案就照做；包里没有才问

### 执行 vs 蒸馏的分工

| 阶段 | 角色 | 行为 |
|---|---|---|
| 蒸馏/生产知识 | 思考者 | 分析、验证、总结、写坑 |
| 编译/消费知识 | 执行者 | 照做、不怀疑、只改可调项 |

**不要在执行阶段当思考者**——这是知识编译的最大误区。

---

## 四、知识包结构（Compiled Knowledge Pack，五层）

```
SKILL.md          → 决策树（先定方向）+ 铁律（硬约束）+ 已验证清单
pipelines.md      → 已验证管线（可复用的端到端流程）
components.md     → 可复用组件（循环、模板、工具）
nodes.md          → 实测值（参数语义、跨体系陷阱、踩坑记录）
可执行脚本         → 与文档配套的真实代码（工具层）
```

### 铁律示例（runninghub-nodes 知识包）

1. Lora/CLIP/VAE 绑定底模，禁止跨体系
2. 节点不独立存在，按管线组装；只编已验证组合，不发明新连接
3. 采样参数绑定体系（跨体系套用 = 未验证）
4. 报错先查实测值（模型名/Lora 跨体系/组件顺序）
5. 提示词体系不混用
6. 网格调参走循环组件，不复制粘贴模块
7. 底模 ≠ 任务专用：换任务可复用底模，但组件必须跟底模走

铁律是"编译产物"的核心：**把专家的判断边界写死，弱模型不会推理，所以要替它推理完。**

---

## 五、与 harness 的关系

知识编译通过 agent harness 在**推理期**注入：
- harness 提供 agent 循环、工具协议、上下文管理（基础设施）
- 知识包提供领域判断（内容）
- 统计评测验证知识包有效性（护栏）

分层原则：
- 知识包的**生产**（蒸馏）：思考者角色，人/AI 在实战中完成
- 知识包的**消费**（编译）：执行者角色，agent 照做
- 知识包的**验证**：统计方法（知识密度 vs 推理密度 → 该不该编译；对照实验 → 编译效果）

---

## 六、验证与演进

### 判断准则（何时照做 vs 何时创新）

| 情况 | 行为 |
|---|---|
| 知识包有明确答案 | 照做（100% 执行） |
| 知识包标注"可调" | 可以改（在安全范围内） |
| 知识包标注"铁律/勿反/勿删" | 绝对不改 |
| 知识包没覆盖 | 问用户（不猜、不发明） |
| 跑通后发现新坑 | 写回知识包（这是蒸馏，属于创新阶段） |

### 正循环

知识包体系会走向"**模式越来越全 → 推理需求越来越少**"的正循环，但永远不可能归零——文档覆盖不到的"意外情况"仍需模型推理，这是知识编译的边界。

---

## 七、落地路径

1. **蒸馏**：从实战中提炼知识（成功案例 → 判定规则 → 铁律）
2. **打包**：组织成可迁移的知识包（五层结构）
3. **编译**：注入给开源小模型消费（直接照做，别思考）
4. **验证**：对照实验量化降本（成本差 vs 质量差）
5. **复制**：第二个、第三个领域知识包，验证方法论可复制性
